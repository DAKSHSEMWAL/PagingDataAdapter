<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PagingDataAdapter" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/titleColor</item>
    </style>

    <style name="Theme.PagingDataAdapter" parent="Base.Theme.PagingDataAdapter" />
</resources>